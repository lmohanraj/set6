#include <stdio.h>
int main(void) {
	int r,n ;
	printf("input");
	scanf("%d",&n);
              if(n%2==0)
              r=n;
              else
              r=n-1;
	 printf(" output=%d",r);
	return 0;
}
